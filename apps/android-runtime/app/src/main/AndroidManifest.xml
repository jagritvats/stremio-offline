<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The addon server binds loopback only, but binding a socket at all needs INTERNET. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            The whole point of Spike 1. Stremio opens the externalUrl of a stream;
            Android dispatches it here. Translucent, excluded from recents and
            without history, so it finishes without Stremio ever leaving the
            foreground (DESIGN section 7).
        -->
        <activity
            android:name=".DeepLinkActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="stremio-offline" />
            </intent-filter>
        </activity>

        <!--
            Android stops background work aggressively, and the addon has to answer
            whenever Stremio asks. A foreground service with a notification is the
            only way to keep a socket listening (DESIGN section 19).
        -->
        <service
            android:name=".RuntimeService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
